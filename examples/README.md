# Examples

Run the reference simulation with:

```bash
arg-simulate --steps 2000 --dt 0.005 --output run.csv
```

The CSV records node states, edge variables, the endogenous collective mode, and intrinsic shortest-path distances at every time step.
